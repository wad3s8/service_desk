pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
        // spring repo — только если нужен
        // maven { url "https://repo.spring.io/release" }
    }
}

rootProject.name = 'service_desk'
